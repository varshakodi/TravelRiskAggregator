# Local dev: run `brew services start postgresql@17` and `createdb risk_aggregator`
# (see README for full setup), then run `python seed.py`.
DATABASE_URL="postgresql://localhost:5432/risk_aggregator"

# Deployed demo: point this at a Supabase (or any managed PostGIS) project instead.
# DATABASE_URL="postgresql://postgres.YOUR_PROJECT:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"

AVIATIONSTACK_KEY="your_key_from_aviationstack.com"

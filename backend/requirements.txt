# Direct dependencies only — pip resolves compatible transitive versions.
# If you add a new `import`, add its package here.

fastapi==0.136.3
uvicorn==0.49.0            # ASGI server that runs `main:app` — not imported, but required to run the app
pydantic==2.13.4

sqlalchemy==2.0.50
alembic==1.18.5
psycopg2-binary==2.9.12    # Postgres DBAPI driver — loaded dynamically by SQLAlchemy from DATABASE_URL, never imported by name
geoalchemy2==0.20.0

apscheduler==3.11.2
python-dotenv==1.2.2
requests==2.34.2
openai==2.41.0
